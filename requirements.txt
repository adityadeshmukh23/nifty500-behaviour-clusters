# Pinned for reproducibility. yfinance in particular changes its
# response schema between minor versions; unpinning it breaks the pipeline.
yfinance==1.7.0
pandas==2.2.3
pyarrow==18.1.0
kaggle==1.6.17
