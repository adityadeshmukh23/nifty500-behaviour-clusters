# Notebooks and tests. The daily pipeline does not need any of this, so the
# scheduled workflow installs requirements.txt alone and stays fast.
-r requirements.txt

numpy==2.5.2
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2
jupyter==1.1.1
pytest==8.4.2
